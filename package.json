{
  "name": "unit3",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^3.4.0",
    "nodemon": "^1.11.0",
    "typescript": "^2.2.2"
  },
  "scripts" : {
    "start": "concurrently --kill-others \"npm run compile\" \"npm run watch\"",
    "compile": "tsc index.ts --watch",
    "watch": "nodemon index.js"
  }
}
